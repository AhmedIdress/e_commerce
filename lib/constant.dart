import 'package:flutter/material.dart';

const Color primaryColor = Color.fromRGBO(0, 197, 105, 1.0);

const Color primyColor = Color.fromRGBO(112, 112, 112, 1.0);
enum DeliveryEnum{
  standard,nextDay,nominated,
}
enum ProcessEnum{
  transit,delivered,
}